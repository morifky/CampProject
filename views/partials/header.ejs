<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
      <!-- Bootstrap CSS -->
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
      <link rel="stylesheet" href="/css/app.css">
    <title>YelCamp</title>
</head>
<body>
        <nav id="mainNavbar" class="navbar navbar-dark bg-dark navbar-expand-lg py-0"> <!--py untuk mengatur padding di sumbu y agar 0-->
            <a href="/campgrounds" class="navbar-brand">YelCamp</a>
            <button class="navbar-toggler" data-toggle="collapse" data-target="#navLinks">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse " id="navLinks">
                <ul class="navbar-nav ml-auto">
                    <% if(!currentUser){ %>
                        <li class="nav-item">
                            <a href="/register" class="nav-link">SIGN UP</a>
                        </li>
                        <li class="nav-item">
                            <a href="/login" class="nav-link">LOGIN</a>
                        </li>
                   <% } else {%>
                        <li class="nav-item">
                            <a href="#" class="nav-link">Welcome, <%=currentUser.username%></a>
                        </li>
                        <li class="nav-item">
                            <a href="/logout" class="nav-link">LOGOUT</a>
                        </li>
                    <% } %> 
                </ul>
            </div>
        </nav>
        <div class="container">
            <%if(error&&error.length>0){%>
            <div style="text-align:center;" class="alert alert-danger">
                <strong><%=error%></strong>
            </div>
            <%}%>
            <%if(success&&success.length>0){%>
            <div style="text-align:center;" class="alert alert-success">
                <strong><%=success%></strong>
            </div>
            <%}%>
        </div>
       
    